---
title: "Legacy iOS Switch"
description: "A registry-ready recreation of the glossy iOS toggle from the classic UI kit, built on Radix Switch and tuned to the original 77 by 27 pixel proportions."
kicker: "Registry component"
order: 2
updatedAt: "2026-03-12"
---

## Installation

Install the direct item URL for the shortest path, or use the Sabraman registry alias once your components config knows about the registry.

<InstallCommand component="ios-switch" />

## Usage

The component wraps Radix Switch behavior in the old iPhone skin, so you keep accessible state management while matching the original skeuomorphic styling.

<ComponentSource
  src="src/components/ios/examples/IosSwitchUsageExample.tsx"
  title="components/ios-switch-demo.tsx"
/>

## API Reference

<AutoTypeTable
  path="registry/default/ios-switch/ios-switch.tsx"
  name="IosSwitchProps"
/>

## Details

- Output: `components/ios-switch.tsx` so the final import stays `@/components/ios-switch`.
- Dependency: Built on top of `@radix-ui/react-switch` while preserving the original glossy labels, thumb, and inset shadow treatment from the Figma source.
