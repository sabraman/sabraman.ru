---
title: "Roundbit"
description: "Use this for pixel-stepped rounded corners when a plain border radius feels too smooth."
kicker: "Registry component"
keywords:
  - "pixel rounded corners"
  - "tailwind pixel corners"
  - "clip-path rounded corners"
  - "shadcn registry utility"
order: 0
updatedAt: "2026-03-21"
---

## Why Use It

- Mirrors Tailwind's `rounded-*` naming so the API feels native.
- Keeps Tailwind and CSS radius semantics, so the resolved radius token stays the same physical size across normal element sizes.
- Supports arbitrary values like `roundbit-[18px]` and step controls like `roundbit-step-4`.
- Lets you stay on normal HTML and CSS for direct clipping, then switch to `RoundbitFrame` plus `roundbit-border-*` when borders or shadows must follow the stepped silhouette.

## Installation

Install the registry item, then use the utility classes for direct clip mode or the frame primitive plus `roundbit-border-*` for layered effects.

<InstallCommand component="roundbit" />

## Quick Start

For clip-only use cases, initialize the runtime on the nearest client subtree instead of mutating the whole document, then add `roundbit-*` classes the same way you would add `rounded-*`.

<ComponentSource
  src="src/components/roundbit/examples/RoundbitQuickStartExample.tsx"
  title="components/roundbit-quick-start-example.tsx"
/>

## Playground

Use the live controls to see how the same radius behaves when you change the corner pattern, step grid, surface size, and frame mode.

<RoundbitPlayground />

## Examples

### Direct Radius Examples

<ComponentPreview
  description="Uniform corners with the default step grid."
  name="roundbit-xl"
  size="compact"
  title="roundbit-xl"
/>

<ComponentPreview
  description="An arbitrary radius without changing the step system."
  name="roundbit-18px"
  size="compact"
  title="roundbit-[18px]"
/>

<ComponentPreview
  description="A tighter stair pattern with the same radius token."
  name="roundbit-xl-step-1"
  size="compact"
  title="roundbit-xl roundbit-step-1"
/>

<ComponentPreview
  description="A chunkier stair pattern with the same radius token."
  name="roundbit-xl-step-4"
  size="compact"
  title="roundbit-xl roundbit-step-4"
/>

### Direct Edge Examples

<ComponentPreview
  description="Only the top edge uses stepped corners."
  name="roundbit-t-xl"
  size="compact"
  title="roundbit-t-xl"
/>

<ComponentPreview
  description="Only the right edge uses stepped corners."
  name="roundbit-r-xl"
  size="compact"
  title="roundbit-r-xl"
/>

<ComponentPreview
  description="Only the bottom edge uses stepped corners."
  name="roundbit-b-xl"
  size="compact"
  title="roundbit-b-xl"
/>

<ComponentPreview
  description="Only the left edge uses stepped corners."
  name="roundbit-l-xl"
  size="compact"
  title="roundbit-l-xl"
/>

### Direct Corner Examples

<ComponentPreview
  description="Only the top-left corner uses the stepped radius."
  name="roundbit-tl-xl"
  size="compact"
  title="roundbit-tl-xl"
/>

<ComponentPreview
  description="Only the top-right corner uses the stepped radius."
  name="roundbit-tr-xl"
  size="compact"
  title="roundbit-tr-xl"
/>

<ComponentPreview
  description="Only the bottom-right corner uses the stepped radius."
  name="roundbit-br-xl"
  size="compact"
  title="roundbit-br-xl"
/>

<ComponentPreview
  description="Only the bottom-left corner uses the stepped radius."
  name="roundbit-bl-xl"
  size="compact"
  title="roundbit-bl-xl"
/>

### Frame Examples

<ComponentPreview
  description="Uniform frame corners with one stable border, outline, and shadow setup."
  name="roundbit-frame-xl"
  size="compact"
  title="RoundbitFrame + roundbit-xl"
/>

<ComponentPreview
  description="A frame example with only the top-left corner stepped."
  name="roundbit-frame-tl-xl"
  size="compact"
  title="RoundbitFrame + roundbit-tl-xl"
/>

<ComponentPreview
  description="A replaced-element wrapper using one outer radius pattern and a chunkier step grid."
  name="roundbit-frame-xl-step-4-image"
  size="compact"
  title="RoundbitFrame + roundbit-xl roundbit-step-4"
/>

## API Reference

`RoundbitFrameProps` gives you a wrapper with an inner `roundbit-content` surface, while `buildRoundbitPaths` and `initRoundbit` cover the utility-only runtime path.

<AutoTypeTable
  path="src/components/roundbit.tsx"
  name="RoundbitFrameProps"
/>

<AutoTypeTable
  path="src/lib/roundbit.ts"
  name="RoundbitPathOptions"
/>

## Notes

- `roundbit-*` is inspired by pixel-corner generators, but it follows Tailwind and CSS radius semantics instead of trying to reproduce generator output exactly.
- Direct mode reads the resolved native `border-radius` back from CSS, so arbitrary values, `%`, and `full` behave like their `rounded-*` counterparts.
- `roundbit-step-2` is the default grid. `roundbit-step-1` and `roundbit-step-4` are style variants, not different radius systems.
- Direct mode is `roundbit-*`. Framed mode is `RoundbitFrame` plus `roundbit-border-*`.
- Frame mode examples are shown one radius pattern at a time so every snippet stays copy-paste ready.
- `roundbit-border-*` is the preferred frame API. Native `border-*` still works on `RoundbitFrame` as a compatibility fallback.
- Frame mode is the safe path for solid borders, uniform dashed/dotted/double borders, zero-offset solid outlines, and spread-free shadows.
- Mixed-side non-solid frame borders fall back to solid rendering instead of trying to fake a broken dashed or double shape.
